import {render} from '@testing-library/vue'
import Welcome from '../../src/components/Welcome.vue'

describe('Welcome Tests', () => {
  it.skip('renders properly Welcome component', async () => {
    render(Welcome)

    // Add here the expected behaviour. Take into account the component needs props passed by
  })
})
